[phases.setup]
nixPkgs = ["python312"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["python build.py"]

[start]
cmd = "uvicorn backend.app.main:app --host 0.0.0.0 --port $PORT"
